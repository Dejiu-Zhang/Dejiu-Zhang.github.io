# 你的 PhD 个人主页 🎓

一个多巴胺配色、现代简洁的学术个人网站，使用 Astro 构建。

## 📁 文件结构说明

```
your-project/
├── src/
│   ├── layouts/
│   │   └── Layout.astro          # 主布局文件（导航栏、页脚、全局样式）
│   └── pages/
│       ├── index.astro           # 主页
│       ├── research.astro        # 研究页面
│       ├── publications.astro    # 出版物页面
│       └── contact.astro         # 联系页面
├── public/                       # 静态文件（放置 CV PDF、图片等）
└── astro.config.mjs             # Astro 配置文件
```

## 🚀 快速开始

### 1. 复制文件到项目

将我提供的文件放到你的项目中：

```bash
# 在你的项目根目录
mkdir -p src/layouts src/pages

# 将文件复制到对应位置：
# - Layout.astro → src/layouts/
# - index.astro, research.astro, publications.astro, contact.astro → src/pages/
```

### 2. 启动开发服务器

```bash
npm run dev
```

打开浏览器访问 `http://localhost:4321`

### 3. 自定义内容

#### 修改个人信息

打开 `src/pages/index.astro`，修改：
- 你的名字（替换 "Your Name"）
- 个人简介
- 研究兴趣描述

#### 修改研究内容

打开 `src/pages/research.astro`，修改：
- 研究项目描述
- 软件包信息
- GitHub 链接

#### 修改出版物

打开 `src/pages/publications.astro`，修改：
- 论文列表
- 作者信息
- 发表期刊

#### 修改联系方式

打开 `src/pages/contact.astro`，修改：
- 邮箱地址
- 办公室地址
- 社交媒体链接

#### 修改导航栏 Logo

打开 `src/layouts/Layout.astro`，找到：
```html
<a href="/" class="logo">YS</a>
```
将 "YS" 改成你的名字首字母。

## 🎨 配色方案

网站使用多巴胺配色：
- 柠檬黄 (`#FFE66D`)
- 珊瑚粉 (`#FF6B6B`)
- 薄荷绿 (`#4ECDC4`)
- 天空蓝 (`#95E1D3`)
- 紫色 (`#C7B3E5`)
- 奶油色背景 (`#FFF8E7`)

如果想修改配色，在 `Layout.astro` 的 CSS 变量中修改：

```css
:root {
    --yellow: #FFE66D;
    --coral: #FF6B6B;
    /* 修改这些颜色值 */
}
```

## 📸 添加个人照片

1. 将照片文件放到 `public` 文件夹
2. 在 `src/pages/index.astro` 中找到 `.image-placeholder` 部分
3. 添加图片：

```html
<div class="image-placeholder">
    <img src="/your-photo.jpg" alt="Your Name" />
    <!-- 保留其他内容 -->
</div>
```

## 📄 添加 CV

1. 将 CV PDF 文件放到 `public` 文件夹，命名为 `cv.pdf`
2. 主页上的"Download CV"链接会自动指向这个文件

## 🌐 部署到 Vercel（免费）

### 1. 推送代码到 GitHub

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/yourusername/your-repo.git
git push -u origin main
```

### 2. 在 Vercel 部署

1. 访问 [vercel.com](https://vercel.com)
2. 用 GitHub 账号登录
3. 点击 "New Project"
4. 导入你的 GitHub 仓库
5. Vercel 会自动检测 Astro 并配置好一切
6. 点击 "Deploy"

几分钟后，你的网站就上线了！Vercel 会给你一个免费的域名（如 `yoursite.vercel.app`）。

### 3. 绑定自己的域名（可选）

在 Vercel 项目设置中：
1. 进入 "Settings" → "Domains"
2. 添加你购买的域名
3. 按照提示在域名商那里设置 DNS 记录
4. 等待几分钟 DNS 生效

## 🛠️ 常用命令

```bash
npm run dev          # 启动开发服务器
npm run build        # 构建生产版本
npm run preview      # 预览生产版本
```

## 💡 进阶定制

### 添加新页面

1. 在 `src/pages/` 创建新的 `.astro` 文件
2. 在 `Layout.astro` 的导航栏添加链接

### 添加博客功能

```bash
# 创建博客文件夹
mkdir -p src/pages/blog
```

然后创建 `.md` 或 `.astro` 文件来写博客文章。

## ❓ 常见问题

**Q: 字体加载失败？**
A: 确保网络连接正常，字体从 Google Fonts 加载。

**Q: 动画不流畅？**
A: 检查浏览器是否启用了硬件加速。

**Q: 样式错乱？**
A: 清除浏览器缓存，重新加载页面。

## 📚 学习资源

- [Astro 官方文档](https://docs.astro.build)
- [CSS 动画教程](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Animations)
- [响应式设计指南](https://web.dev/responsive-web-design-basics/)

---

祝你拥有一个漂亮的个人主页！如果有任何问题，随时问我 😊
